/** If you get all records, You can use this paging querry option. */
export interface IPaging {
    /** Getting records from this number. */
    offset?: number;
    /** Maximum acquisition number. */
    limit?: number;
}
